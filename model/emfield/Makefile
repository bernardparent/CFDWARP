all: src

src:
	( cd .active ; make src )
	( cd .active ; make arc )

clean:
	( cd .active ; make clean )

cleanall:
	( cd _Epotential-Bfixed ; make cleanall )
	( cd _Epotential-Te ; make cleanall )
	( cd _Epotential ; make cleanall )
	( cd _none ; make cleanall )
	( cd doc ; make clean )

# DO NOT DELETE THIS LINE -- make depend depends on it.
